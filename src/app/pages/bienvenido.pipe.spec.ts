import { BienvenidoPipe } from './bienvenido.pipe';

describe('BienvenidoPipe', () => {
  it('create an instance', () => {
    const pipe = new BienvenidoPipe();
    expect(pipe).toBeTruthy();
  });
});
